ofdocbuild:
    build: .
    volumes:
        - .:/ofDocumentationGenerator
    #command: /bin/bash -c "cd /openframeworks/; git pull origin master; python /ofDocumentationGenerator/src/documentation_update.py /openframeworks/; python /ofDocumentationGenerator/src/generate_site.py;"
    #command: /bin/bash -c "cd /openframeworks/; git pull origin master; python /ofDocumentationGenerator/src/generate_site.py;"
web:
    image: nginx
    volumes:
        - ./_site:/usr/share/nginx/html:ro
    ports:
        - "80:80"
